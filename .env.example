# Base de données PostgreSQL (Railway)
DATABASE_URL="postgresql://user:password@host:5432/buildflow"

# JWT
JWT_SECRET="change-this-secret-in-production"
JWT_EXPIRES_IN="7d"

# Serveur
PORT=3000
NODE_ENV=development

# CORS — URL du frontend Angular
FRONTEND_URL="http://localhost:4200"

# Mot de passe par défaut créé lors de la création d'un compte utilisateur
DEFAULT_PASSWORD="Buildflow2026!"

# ── Email SMTP (envoi des identifiants aux nouveaux utilisateurs) ──
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=kouamejeantrinite@gmail.com
EMAIL_PASS=gcuf zyvp ztlv ythc
EMAIL_FROM="BuildFlow <kouamejeantrinite@gmail.com>"
